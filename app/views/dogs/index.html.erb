<h1 class="p-2">This is all Dogs</h1>

<div class="d-flex">


<div class="container">
<div class="row m-2">
<div class="p-2">
<%= link_to "Create Dog", new_dog_path, class:"btn btn-primary" %>
<%= link_to "Create Dog", new_dog_path, class:"btn btn-primary" %>
</div>
</div>
  <% @dogs.each do |dog| %>
    <div class="row m-2">
      <div class="card-product">
        <% if dog.photo.attached? %>
        <%= cl_image_tag dog.photo.key, crop: :fill %>
        <% end %>
      <div class="card-product-infos">
        <h2><%= dog.name %></h2>
        <p><%= dog.breed %></p>
      </div>
  </div>
</div>
      <% end %>
</div>
<div>
</div>
</div>
